<section class="card">
  <a href="/comment/{{id}}">
    <header>{{title}}</header>

    <div class="card-body">
      {{!-- <img src="/images/{{filename}}" alt="{{description}}"> --}}
      <p>By: {{username}}</p>
    </div>
  </a>

</section>
